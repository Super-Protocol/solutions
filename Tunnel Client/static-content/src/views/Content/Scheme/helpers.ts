/* eslint-disable max-len */
export const lines = [
  'Website data is deployed into the confidential Trusted Execution Environments (TEE’s) across one or multiple compute providers. A TEE is an isolated area in RAM that is protected by the CPU from any unauthorized access, including the owner of the machine. We assume by default that all machines are untrusted and require confidentiality.',
  'The user browses to a website and the DNS automatically determines the IP for access, opening a tunnel between the user and the data, secured by the TLS / mTLS protocols.',
  'The more tunnels there are - the more decentralized and resistant to DDOS attacks is the hosting.',
];

export const title = 'How It Works?';
